{
  "name": "${KS_NAME}-datasource",
  "description": "Data source for knowledge source '${KS_NAME}'",
  "type": "sharepoint",
  "credentials": {
    "connectionString": "${SP_CONNECTION_STRING}"
  },
  "container": {
    "name": "${SP_CONTAINER}",
    "query": ${SP_QUERY_JSON}
  }
}
